{
  "date": "2026-08-20",
  "total_feeds": 92,
  "success_feeds": 84,
  "total_articles": 2513,
  "filtered_articles": 9,
  "articles": [
    {
      "title": "Issues in the Repo",
      "title_zh": "把 Issue 随 Git 仓库同步的五种方案",
      "link": "https://nesbitt.io/2026/08/20/issues-in-the-repo.html",
      "pub_date": "2026-08-20T09:00:00.000Z",
      "summary": "## 一句话总结\n\n把 issue 和 review 数据放进版本库并不难，真正的取舍在于：你愿意让它们以何种 Git 对象存在，以及为离线协作、可读性、同步安全和冲突处理付出什么代价。\n\n## 正文\n\n### GitHub 宕机暴露的，不是代码丢失，而是上下文丢失\n\n这篇文章从一次 GitHub 故障切入：git 操作、Actions 和 issue tracker 同时不可用数小时。代码暂时不可访问固然烦人，但通常还不至于让项目停摆。毕竟每个贡献者的本地 clone 都有一份完整代码。真正脆弱的是 issues 和 PR 讨论，它们往往只存在 GitHub 的数据库里。换句话说，代码是分布式的，项目协作上下文却仍可能是中心化且不可克隆的。\n\n作者因此追问一个很直接的问题：有没有工具能把 issue、评论和 review 数据也放进 repository，让它们随 `git clone`、`git fetch` 和 `git push` 一起流动？结果发现，这并不是新鲜想法。过去约二十年里，已经有不少项目尝试过。它们的区别不主要在 UI，而在于数据到底落在 working tree、orphan branch、`git notes`、custom refs，还是干脆由 VCS 原生支持。后面的比较也围绕几个关键问题展开：默认 clone 能否拿到数据、离线并发修改如何合并、没有专用工具时能否阅读、普通 bare Git host 是否能保存，以及身份如何定义。\n\n### 最朴素的方案：把 issue 当作代码旁边的文件\n\n最早的一条路线非常直白：每个 issue 就是一个文本文件，或者一个文件目录，直接提交到源码仓库。2005 年的 Bugs Everywhere、2008 年的 ditz 都采用过这类模型。近一些的 GitRoot 甚至据此构建了完整 self-hosted forge，把 issue 存成 `issues/<id>-<slug>.md`，状态放在 front matter 中。还有更极简的实践，直接复用 changelog fragment 目录当作轻量工作项，甚至不需要专门工具。\n\n这个设计的优点几乎全是 Git 白送的。普通 `git clone` 会拿到 issue，`grep -r` 可以搜索，`cat` 可以读取，`git bundle` 可以打包，任何 dumb HTTP host 或 cgit 也都能提供访问。并发编辑同一个 issue 时，Git 的三方合并就是合并机制。两个人各自在文件末尾追加评论，通常能自动合并；两人都改标题，则产生正常文本冲突，由人手解决。身份也不必另起炉灶，`git blame`、commit author 和项目既有的 commit signing 就足够表达“是谁创建或修改了它”。\n\n但问题也很明显：issue 的可变状态和代码历史被塞进了同一条 commit graph。繁忙项目的 `git log` 会混入大量“修改 issue #34”之类的提交，`git bisect` 也可能踩到这些纯元数据提交。更麻烦的是分支语义：创建 feature branch 时，你也 fork 了 issue 的状态。某个 issue 在分支上关闭，切回 `main` 又会显示打开。给提交加固定前缀，再用 `--invert-grep` 可以缓解日志噪音，但无法解决“元数据跟着代码分支走”这个结构性问题。\n\n### Orphan branch 把 issue 历史从 main 中搬走了\n\n于是下一种设计把 issue 放到 orphan branch。它是一个没有父提交的分支，和 `main` 不共享历史，也不会出现在普通工作区 checkout 中。2008 年的 ticgit 和后来的 ticgit-ng 就把 issue 放在名为 `ticgit` 的分支上。新的 haxy 则把事件日志放到 `refs/heads/haxy/events`，每个 commit 的 tree 为空，JSON 事件写进 commit message。服务端通过回放这些事件，计算 issue 的当前状态。\n\n这样做解决了一个核心问题：`main` 的日志终于不再混入 issue 操作。工具可以通过 plumbing 或第二个 worktree 读取和写入这个分支，而并发修改仍然沿用 Git 的文件合并机制。由于 ref 位于 `refs/heads/` 下，默认 `git clone` 会自动抓取它。这是 orphan branch 相比 `git notes` 和 custom ref 的最大优势。ticgit 甚至把 `STATE_open`、`TAG_attach`、`ASSIGNED_<email>` 等状态编码进文件名，所以单靠 `git ls-tree` 就能看出 issue 状态。\n\n不过，orphan branch 的风险恰好来自它“看起来太像普通分支”。它会出现在 `git branch -a` 中，某位维护者很可能把 `ticgit` 当成过期分支删掉。来自 `--single-branch` clone、只拉取 `main` 的 CI，或者执行 `git push --prune`、`git push --mirror`，都可能在远端误删它。多数 forge 的 branch protection 又依赖名字匹配，除非团队专门配置，否则 `ticgit` 未必受保护。一旦这个 ref 被删，整段断开的对象图会变成 unreachable，后续 `git gc --prune` 可能彻底清除数据，而普通 bare host 通常也没有 server-side reflog 可供恢复。\n\n### git notes 适合“附着在提交上的协作信息”\n\nGit 自带的 `git notes` 走的是另一条路：把任意 blob 附着到已有 commit 上，数据存储在 `refs/notes/<namespace>`。note ref 本身指向一个普通 commit，其 tree 将被注释的 commit SHA 映射到对应 blob。因此，新增或修改 note 本身也有 commit，历史并不会丢失。Google 已归档的 git-appraise 就在这套机制上实现了分布式 code review。它将 review request、讨论串和 CI 状态分别存进 `refs/notes/devtools/reviews`、`discuss` 和 `ci`。\n\nnotes 的并发合并比纯文本文件更有针对性。Git 原生提供 `git notes merge`，其中 `cat_sort_uniq` 策略会逐行拼接并去重。也正因为如此，git-appraise 把每个 JSON object 放成一行，而不是保存成一个格式化的大 JSON 文档。任何 Git host 都可以接受这种 ref，`git log --notes='*'` 还能把 note 内联显示到提交历史中。通过 `git bundle create backup.bundle --all`，notes 也能被一起打包备份。\n\n但这里有两个边界。第一，默认 clone 不会抓取 `refs/notes/*`，用户需要手动配置 `fetch = +refs/notes/*:refs/notes/*`。第二，note 必须挂在一个既有 commit 上，所以它天然适合 code review、CI 结果和“这次提交的讨论”。对于一个不对应任何特定 commit 的 bug report，这个模型就有些别扭。身份则来自 notes commit 的 author，并可由 payload 补充；例如 git-appraise 会在 JSON 中记录 requester email。\n\n### Custom refs 让 issue 成为独立的、可合并的数据模型\n\n目前仍活跃的项目，大多选择把数据存进 `refs/heads/` 之外的 custom ref namespace。这样它不会出现在 `git branch` 里，不会污染 working tree，同时工具可以自由定义对象布局。git-bug 是其中较成熟的例子：每个 bug 是 `refs/bugs/<id>` 下的一条 commit chain。每个 commit 的 tree 中包含 JSON 格式的 `ops` blob，以及编码 Lamport clock 的零字节 marker file。身份则被建模为独立的 `refs/identities/<id>` 链。\n\n这条路线不止用于 issue。Gerrit 的 NoteDb 将每个 change 的 review metadata 存入 `refs/changes/NN/NNNN/meta`，并且从 Gerrit 3.0 起支撑了 Google 的 code review，而不依赖 SQL 数据库。Radicle 则把 issues 和 patches 放进各 peer 的 `refs/namespaces/<nodeid>/refs/cobs/`，再通过 gossip protocol 合并。git-native-issue 使用 `refs/issues/<uuid>`，让 commit tree 保持为空，元数据改放到 commit-message trailers 中，例如 `State: open` 与 `Labels: bug`。GitHub 和 GitLab 事实上也在使用类似层次暴露 PR/MR 的代码部分，例如 `refs/pull/N/head` 和 `refs/merge-requests/N/head`，只是这些 refs 对客户端是只读的。\n\n这种方案的关键，不是“把 JSON 放到 Git 里”，而是把每一次修改表达成追加操作。两个用户离线编辑同一个 bug，最终会产生两个 divergent tip。工具不会要求用户解决文本冲突，而是回放双方 operation log，并依据 Lamport clock 合并状态。代价是，当两人离线修改同一字段时，系统会按 clock 顺序自动选出胜者，而不是显式呈现冲突。说白了，这把“人手合并冲突”换成了“数据模型定义合并语义”。\n\n代价也不小。默认 clone 会跳过这些 refs，因此所有工具都得先向 `.git/config` 注入 refspec，例如抓取 `refs/bugs/*` 和 `refs/identities/*`。raw object 虽然能通过 `git cat-file` 检查，但人类通常无法不借助工具直接读懂。identity 也必须进入自己的数据模型：git-bug 保存 versioned identity object，Radicle 则从节点的 Ed25519 key 派生身份。好消息是，git-bug 已提供 GitHub、GitLab 和 Jira bridge，因此现有 tracker 可以镜像到 `refs/bugs/`，在 forge 宕机时仍作为可用副本。\n\n### Fossil 说明：最彻底的解法是让 VCS 原生理解 ticket\n\n最后，文章提到一种更彻底的选择：不要在 Git 上搭 issue tracker，而是让 VCS 从设计之初就把 ticket 当作一等对象。Fossil 就是代表。它把 check-in、ticket、wiki、forum thread 和 technote 都存进同一个 content-addressed store，并将整个 repository 打包成单个 SQLite 文件。ticket 由一组不可变 artifact 构成，每个 artifact 声明对若干命名字段的修改。当前状态不是更新某条数据库记录，而是按时间顺序应用所有具有相同 ticket id 的 artifact。\n\nFossil 的 card format 让这种模型保持可审计。`D` 表示时间戳，`J` 表示字段赋值，`K` 指向被修改的 ticket，`U` 表示用户，`Z` 则是前面内容的 MD5。同步时，节点交换对方缺少的 artifact。并发修改同一 ticket 时，双方 artifact 都会保留，而不是覆盖彼此。由于每条 `J` 只更新一个字段，实际合并策略是按字段 last-writer-wins，且不会产生 Git 风格的 conflict marker。\n\n这也是全文逐渐显现出的结论：issue 数据“是否在仓库里”，只是第一层问题。更关键的是它是否随默认 clone 到达、是否会被误删、是否能在没有专用工具时审阅，以及冲突究竟交给人、Git，还是交给领域模型。工作区文件模型最透明，却与代码分支纠缠。orphan branch 保持 clone 友好，却容易被当成普通分支误操作。notes 和 custom refs 更干净、更适合专用协作模型，但需要显式配置和专门工具。",
      "source_name": "nesbitt.io",
      "score": 15,
      "depth": 8,
      "novelty": 7,
      "category": "engineering",
      "keywords": [
        "git",
        "issue tracking",
        "custom refs",
        "git notes"
      ],
      "rank": 1
    },
    {
      "title": "smolmachines / smolvm as a sandbox for untrusted Python & JavaScript",
      "title_zh": "用 smolvm 隔离不可信 Python 与 JavaScript",
      "link": "https://simonwillison.net/2026/Aug/19/smolmachines-untrusted-sandbox/",
      "pub_date": "2026-08-19T23:16:00.000Z",
      "summary": "## 一句话总结\n\n这篇研究验证了 `smolvm 1.8.3` 可以用硬件隔离的 VM，而非共享内核的容器，构建一个能安全执行不可信 Python 和 JavaScript 数据转换任务的本地 sandbox。\n\n## 正文\n\n### 目标很具体：让用户代码只能处理数据，不能碰宿主机\n\n这项研究要解决的不是“能不能跑 Python”，而是“能不能安全地跑用户提交的 Python 和 JavaScript”。目标场景是数据转换：用户给出一段代码和输入文件，系统执行后产出指定结果。问题在于，这类代码天然不可信，可能包含 `while true` 之类的无限循环，也可能试图耗尽内存、访问网络，或者读取不该读取的文件。  \n所以需要的 sandbox 不只是隔离进程，而是要能明确限制 CPU 时间和 RAM。文件系统也必须收紧，只允许代码读取指定输入、写入指定输出。网络则应该默认彻底关闭。说白了，这是一套“允许计算，但不允许越界”的执行环境。\n\n### 为什么看中 smolvm：隔离边界从容器换成 VM\n\n研究测试的对象是 `smolmachines` / `smolvm 1.8.3`。它的关键吸引力在于，隔离单位是 hardware-isolated VM，而不是与宿主共享 kernel 的 container。对于执行不可信代码，这个架构选择很重要：容器的隔离最终仍依赖共享内核，而 VM 提供了更强的边界。  \n测试结果表明，smolvm 能以本地离线镜像运行，不需要在执行时拉取网络资源。它还支持完全禁网执行，以及 CPU、RAM 和存储配额。时间限制由 guest 侧强制执行，而不是只依赖宿主发信号终止进程。再配合 `--unprivileged`，以及只读 input mount 和可写 output mount，所需的权限模型基本都具备了。\n\n### 关键不是“能限制”，而是这些限制真的被验证了\n\n这篇研究并没有停留在功能列表，而是实际跑了一组测试来验证 sandbox 行为。测试确认了 no-network execution 按预期工作，CPU 和内存限制也能生效。针对无限循环等场景，guest-enforced timeout 能够终止超时任务。  \n文件访问模型同样符合目标：输入目录以只读方式挂载，任务可以将结果写入单独的输出目录。存储配额也可用，因此用户程序不能无限制地膨胀磁盘占用。最终结论是，smolvm 很适合承载这种“不可信脚本做受控数据处理”的工作负载。性能上，冷启动约为 `0.6–1.5 秒`，而 warm execution 约为 `50 ms`，这让它不只是安全方案，也具备实际交互式使用的可行性。\n\n### 研究过程先撞上了嵌套虚拟化的墙\n\n有意思的是，执行研究的 Claude Fable 5 一开始并不能直接运行 smolvm。它所在的 Claude Code for web 环境本身就是一个 Firecracker guest，系统是 `Linux 6.18.5-fc-v20`，配置为 `4 vCPU` 和 `15GB RAM`。但这个环境没有 `/dev/kvm`，CPU 也没有 `vmx/svm` flags。  \n这意味着它无法使用 nested virtualization，直接执行 `smolvm machine run` 会报出预期的错误：`kvm not available`。如果研究到这里停止，结论就只能是“当前环境无法测试”。但问题并不在 smolvm，而在研究环境没有暴露 KVM。于是 Fable 没有试图绕过安全限制，而是换了一个具备必要硬件能力的执行环境。\n\n### Plan B 选中了 GitHub Actions，并把真实测试跑起来了\n\nFable 的替代方案是 GitHub Actions 的 Ubuntu runner，因为它暴露了 `/dev/kvm`。它在当前分支创建了一个临时 workflow，安装 smolvm，并直接执行测试脚本。这样，测试仍然是在支持 KVM 的真实环境中运行，而不是用 mock 或纯理论分析替代。  \n日志被收集后，临时 workflow 会在最终提交中移除，避免把一次性研究基础设施永久留在仓库里。这种做法的价值不只是“找到了一台能跑 VM 的机器”。更关键的是，它把环境限制转化成了可复现的 CI 实验。文章最后也把这件事作为 Claude Fable 主动性的一个例子：遇到当前环境不支持的能力时，它没有停在失败信息上，而是自己寻找了满足前提条件的验证路径。",
      "source_name": "simonwillison.net",
      "score": 14,
      "depth": 7,
      "novelty": 7,
      "category": "security",
      "keywords": [
        "smolvm",
        "smolmachines",
        "Firecracker",
        "sandbox"
      ],
      "rank": 2
    },
    {
      "title": "Use the built-in GELU, don't roll your own!",
      "title_zh": "内置 PyTorch GELU 让 GPT-2 训练快20%",
      "link": "https://www.gilesthomas.com/2026/08/built-in-gelu",
      "pub_date": "2026-08-20T02:00:00.000Z",
      "summary": "## 一句话总结\n\n在 GPT-2 small 训练中，把手写的 `tanh` GELU 换成 PyTorch 内置 `nn.GELU`，即使数学近似完全相同，吞吐也从约 21,000 tps 跃升到 25,000 tps，白捡了 20% 性能。\n\n## 正文\n\n### 一次“顺手重构”，意外找到了 20% 的训练性能\n\n这篇文章的起点很朴素：作者在给自己的 PyTorch GPT-2 实现加 Mixture-of-Experts（MoE）支持时，顺手清理了一点旧代码。旧代码来自 Sebastian Raschka 的《Build a Large Language Model (from Scratch)》，其中自己实现了一个 `GELU` 模块。作者原本只是想把它替换成 PyTorch 原生的 `nn.GELU()`，减少一点看起来像样板代码的维护负担。结果，MoE 代码突然变快了。起初这很像一次测量幻觉，但重新跑了三轮各 20 分钟的训练后，结论非常清楚：不是幻觉。手写 GELU 的训练吞吐约为 20,920 tokens/s，而 PyTorch 内置 GELU 达到了 25,134 tokens/s。也就是说，只改一个 activation 的实现，整体训练就快了约 20%。\n\n### 问题不在 GELU 的数学，而在它怎样落到 GPU 上\n\n这里最有意思的地方是，作者手写的 GELU 并不是什么离谱的实现。它使用的是常见的 `tanh` 近似：\n\n```python\n0.5 * x * (\n    1 + torch.tanh(\n        torch.sqrt(torch.tensor(2.0 / torch.pi))\n        * (x + 0.044715 * torch.pow(x, 3))\n    )\n)\n```\n\n而 PyTorch 的 `nn.GELU(approximate=\"tanh\")` 使用的也是同一套近似数学。换句话说，模型算的东西本质上没有变，数值近似方式也没有变。但手写版本会暴露成一串独立的 PyTorch tensor 操作：`pow`、乘法、加法、`tanh` 等等。内置版本则可以直接走框架针对 GELU 优化过的实现。说白了，慢的不是 GELU 公式本身，而是把一个公式拆成许多通用操作后带来的额外开销。\n\n### 连“精确 GELU”都没有输给手写近似版\n\n作者没有只测一个 `nn.GELU()`，而是把关键变量拆开了。第一组是旧的手写 `tanh` 近似 GELU，20 分钟后的平均速度为 20,920 tps。第二组换成默认的 `nn.GELU()`，得到 25,134 tps。需要注意，PyTorch 默认 GELU 用的是精确计算，而不是手写代码所用的 `tanh` 近似。直觉上，精确版本似乎应该更慢，但实际结果反而快了 20%。这已经说明，kernel 实现和算子调度带来的差异，远远压过了“公式看起来更复杂”这件事。\n\n### 同样的 tanh 近似，内置实现依然快 20%\n\n为了排除“精确公式与近似公式不同”这个因素，作者又进行了第三组实验：\n\n```python\nnn.GELU(approximate=\"tanh\")\n```\n\n这组使用与手写版本相同的 `tanh` 近似，20 分钟后的吞吐是 25,142 tps。它和默认 `nn.GELU()` 的 25,134 tps 基本完全一样。于是实验把结论钉死了：性能差异不是由近似策略造成的。相同数学、几乎相同数值行为下，内置实现比 Python 层拼出来的运算图更高效。对于这个配置而言，直接使用框架提供的 fused 或优化 kernel，才是决定性因素。\n\n### 12 个 GELU，为什么能拖慢整个 GPT-2 训练？\n\n作者训练的是 GPT-2 small 风格模型，共 12 层。每层只有一个 GELU，处理的张量形状是 `(batch_size, seq_len, 4 * d_emb)`，具体为 `(6, 1024, 3072)`。看上去这并不是一个特别夸张的数量，尤其模型里还有完整的 self-attention、投影层和反向传播。可整体吞吐从约 21k 升到 25k tps，意味着旧实现引入的额外成本约占总训练时间的 17%。这还是一个下界，因为内置 GELU 并不代表 GELU 本身完全不耗时。真正令人警醒的地方在于：一个位于每层 MLP 中间的 activation，如果实现方式不对，足以明显吞掉 Transformer 训练预算。\n\n### JAX 比 PyTorch 快，原来不全是 JIT 的功劳\n\n这次发现还顺带解释了作者此前一个困惑。作者的 JAX 实现使用内置 `jax.nn.gelu`，训练速度约为 24,000 tps。PyTorch 实现此前只有约 21,000 tps，尽管它启用了 AMP，也就是能在安全的地方使用 16-bit 计算的 Automatic Mixed Precision。作者此前测得 AMP 可将 PyTorch 从 15,402 tps 提升至 19,797 tps，因此 JAX 在 full 32-bit 下仍比 PyTorch AMP 快，曾让他把原因主要归于 JAX 的 JIT。现在看，PyTorch 版本的手写 GELU 人为拉低了成绩，抵消了一部分 AMP 带来的收益。换成内置 GELU 后，PyTorch 已达到约 25,000 tps，反而略高于此前 JAX 的约 24,000 tps。\n\n### 后续值得测 torch.compile，但当下答案已经足够明确\n\n作者并没有声称手写 GELU 在任何条件下都必然更慢。比如 JAX 的 JIT 可能会把那串逐元素操作编译、融合成高效 GPU kernel，因此 JAX 中手写和内置的差距可能更小。PyTorch 的 `torch.compile` 也可能做到类似的图编译与算子融合，届时内置 GELU 的优势是否还存在，值得专门测试。还可以继续测非 AMP 的 PyTorch 加内置 GELU，进一步拆解 AMP、GELU 实现和编译优化各自的贡献。但这些都是后续研究题。眼下这篇文章给出的工程结论很直接：只要框架已有成熟的内置模块，就不要为了“从零实现”的完整感去手搓它。",
      "source_name": "gilesthomas.com",
      "score": 13,
      "depth": 7,
      "novelty": 6,
      "category": "ai-ml",
      "keywords": [
        "PyTorch",
        "GELU",
        "JAX",
        "training throughput"
      ],
      "rank": 3
    },
    {
      "title": "Anubis continues to expose new ways people configure webservers",
      "title_zh": "Anubis 用 blob Worker 减少挑战页请求",
      "link": "https://xeiaso.net/notes/2026/anubis-csp-worker-hell/",
      "pub_date": "2026-08-19T00:00:00.000Z",
      "summary": "## 一句话总结\n\nAnubis 为了减少 Proof of Work challenge 对服务器的并发请求，改用“`fetch` 一次 Worker 源码再生成 `blob:` Worker”的方案，却发现严格 CSP 会让 Worker 在异步阶段失败，只能保留更耗请求的降级路径。\n\n## 正文\n\n### CSP 的本意是收紧权限，代价是功能会以奇怪方式失效\n\n写 Web 应用时，开发者不得不信任浏览器，但这种信任从来不是无条件的。浏览器如今暴露了 WebUSB、Built-in AI 等越来越多的能力，管理员自然希望关闭站点不需要的功能。CSP，也就是 Content-Security-Policy，正是做这件事的工具：默认禁用，再按需放行。Anubis 文档里的推荐策略就很典型，`default-src 'none'` 先一刀切，然后只允许同源脚本、样式、图片、字体和 `fetch()` 请求。它还特意允许 `worker-src 'self' blob:`，因为后面的 Worker 优化方案正依赖这个看似不起眼的 `blob:`。\n\n### 最难调试的地方：Worker 构造成功，不代表它真的能运行\n\n这里有个容易踩坑的细节：如果 CSP 禁止 Worker 从 `blob:` URI 加载脚本，`new Worker(blobURL)` 并不会同步抛异常。代码可以正常创建 `Blob`，通过 `URL.createObjectURL()` 得到 URL，再把它传给 `new Worker()`。真正的失败发生在浏览器创建后台线程之后，因此只能从异步的 `w.onerror` 回调里捕获。换句话说，传统的 `try/catch` 只能覆盖 Worker 构造阶段，覆盖不了 CSP 拦截脚本加载的阶段。对于依赖 Worker 创建结果做 fallback 的程序来说，这意味着控制流必须重新设计，不能再把“没抛异常”当作“Worker 已可用”。\n\n### Anubis 的并行 PoW，原本会把请求压力放大\n\nAnubis 不希望用户长时间看到 challenge 页面，因此会并行执行 Proof of Work 检查，尽快让验证页消失。这很符合现代 CPU 的特点：过去十年的性能提升，更多来自多核而不是单核。问题是，普通 Worker 的创建方式会让每个 Worker 都单独请求一次脚本，例如 `new Worker(\"/static/js/worker/test1.mjs\")` 会触发一次 `GET`。Anubis 会根据 `navigator.hardwareConcurrency` 决定并发数，默认使用 CPU 线程数的一半，最低为 1。于是当服务器本就被大量 scraper 压垮时，一个正常用户完成 challenge，可能额外触发多达 16 个 Worker 脚本请求。\n\n### 先 fetch 一次，再用 blob 分发给所有 Worker\n\n因此，Anubis 的新思路其实很直接：不要让每个 Worker 分别去服务器取代码。它先用普通的同源 `fetch()` 加载一次 Worker 源码，再将源码封装成 `Blob`，最后通过 `blob:` URI 创建多个 Worker。这样，原来随着 Worker 数量线性增长的 HTTP 请求，被压缩成一次请求加多个本地 Worker 初始化。这个设计还有一个现实好处：如果 Worker 加载需要重试，重试逻辑不必散落在每次 `new Worker()` 的外围。说白了，Anubis 把“从服务器拿代码”和“并行启动计算线程”拆开了，减少了挑战页本身对过载服务器造成的二次压力。\n\n### 但 CSP 不允许 blob 时，只能接受更差的降级路径\n\n问题是，并非所有管理员的 CSP 都允许 `blob:` URI。为了兼容这种配置，Anubis 仍然保留了旧的“每个 Worker 各自请求脚本”的逻辑。这个 fallback 不仅增加服务器压力，还要求额外测试，因为 `blob:` Worker 的失败是异步发生的，不能只靠构造时的异常处理判断。文章也解释了为何这种容错可以相对宽松：PoW 的核心大多只是数学计算，每个 Worker 分到 nonce 空间中的一条“lane”。丢掉一两个 Worker 会跳过它们负责的 nonce 区间，但只要还有 Worker 在运行，通常仍能找到解；未来或许会支持从失败位置重启 Worker，但目前不在范围内。最终，这篇文章揭示的不是某个神奇优化，而是现代 Web 开发里更常见的现实：安全策略、浏览器异步行为、服务器负载和客户端兼容性，会在最边缘的地方一起制造难以复现的 bug。",
      "source_name": "xeiaso.net",
      "score": 12,
      "depth": 6,
      "novelty": 6,
      "category": "engineering",
      "keywords": [
        "Anubis",
        "Content-Security-Policy",
        "Web Workers",
        "blob URI"
      ],
      "rank": 4
    },
    {
      "title": "Our Servants Will Do That For Us",
      "title_zh": "AGI 会同时吞没苦工与有意义的职业",
      "link": "https://borretti.me/article/our-servants-will-do-that-for-us",
      "pub_date": "2026-08-20T00:00:00.000Z",
      "summary": "AGI 不会沿着“先消灭苦工、保留创造性职业”的路径发展：可自动化性的边界并不等同于苦工边界。人们购买服务时又普遍偏好更便宜、快速且免于与人打交道的机器替代品，软件、数学与艺术都难以天然幸免。未来仅靠人际关系维系的职业，也可能被按需生成的 AI 内容持续侵蚀。",
      "source_name": "borretti.me",
      "score": 11,
      "depth": 5,
      "novelty": 6,
      "category": "opinion",
      "keywords": [
        "AGI",
        "automation",
        "labor economics",
        "solipsism"
      ],
      "rank": 5
    },
    {
      "title": "Conceptual integrity and counting lines of code",
      "title_zh": "AI 编码提速后更稀缺的是概念完整性",
      "link": "https://simonwillison.net/2026/Aug/19/conceptual-integrity-and-counting-lines-of-code/",
      "pub_date": "2026-08-19T22:46:07.000Z",
      "summary": "编码代理可让单个工程师每天产出约千行经调试的代码，远高于传统开发中常见的 50—200 行；但代码量暴涨并不等于团队可缩减。真正的瓶颈转为理解、审查和维护大量变更的认知容量。低成本加功能还会让系统像 Winchester Mystery House 一样不断添建，破坏《人月神话》所说的概念完整性，因此团队需要以架构纪律替代过去由开发工时自然形成的约束。",
      "source_name": "simonwillison.net",
      "score": 8,
      "depth": 3,
      "novelty": 5,
      "category": "opinion",
      "keywords": [
        "coding agents",
        "conceptual integrity",
        "lines of code",
        "The Mythical Man-Month"
      ],
      "rank": 6
    },
    {
      "title": "On wrapping a callable in a lambda that just calls it with the same parameters",
      "title_zh": "C++ 无需为 callable 再套一层 lambda",
      "link": "https://devblogs.microsoft.com/oldnewthing/20260819-00/?p=112624",
      "pub_date": "2026-08-19T14:00:00.000Z",
      "summary": "C++ 中 lambda 本质是带有 operator() 的闭包对象；若队列 API 接受任意 callable，直接以 std::forward<Lambda>(lambda) 传入即可。额外包装只会增加一个持有原对象并转调 operator() 的闭包层。仅当需要改写参数类型、传递方式或调用语义时，包装才有实际价值。",
      "source_name": "devblogs.microsoft.com/oldnewthing",
      "score": 7,
      "depth": 4,
      "novelty": 3,
      "category": "engineering",
      "keywords": [
        "C++",
        "lambda",
        "callable",
        "perfect forwarding"
      ],
      "rank": 7
    },
    {
      "title": "Adobe Systems IPO August 20, 1986",
      "title_zh": "Adobe 如何从 PostScript 走向软件巨头",
      "link": "https://dfarq.homeip.net/adobe-systems-ipo-august-20-1986/?utm_source=rss&#038;utm_medium=rss&#038;utm_campaign=adobe-systems-ipo-august-20-1986",
      "pub_date": "2026-08-20T11:00:07.000Z",
      "summary": "",
      "source_name": "dfarq.homeip.net",
      "score": 6,
      "depth": 3,
      "novelty": 3,
      "category": "other",
      "keywords": [
        "Adobe",
        "PostScript",
        "desktop publishing",
        "IPO"
      ],
      "rank": 8
    },
    {
      "title": "Quoting Jeremy Morrell",
      "title_zh": "LLM 与沙箱催生可扩展软件",
      "link": "https://simonwillison.net/2026/Aug/19/jeremy-morrell/",
      "pub_date": "2026-08-19T22:56:31.000Z",
      "summary": "",
      "source_name": "simonwillison.net",
      "score": 5,
      "depth": 1,
      "novelty": 4,
      "category": "opinion",
      "keywords": [
        "LLMs",
        "extensible software",
        "sandboxing",
        "web"
      ],
      "rank": 9
    }
  ]
}