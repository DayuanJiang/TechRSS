---
import { listDigestDates } from '../../lib/data';

const base = import.meta.env.BASE_URL.replace(/\/?$/, '/');
const dates = listDigestDates();
const latest = dates[0];
---
<meta http-equiv="refresh" content={`0;url=${base}day/${latest}/`} />
